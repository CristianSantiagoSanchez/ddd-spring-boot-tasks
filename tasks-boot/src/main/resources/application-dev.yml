spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/tasks
    username: root
    password: plexus
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
